import React from "react";
import { Button } from "semantic-ui-react";

export default () => {
  return (
    <Button
      labelPosition="right"
      icon="right chevron"
      content="Next"
      primary
      type="submit"
      size="large"
    />
  );
};
